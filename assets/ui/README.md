# UI / Reward Icons

No files exist yet at this path. This folder is currently empty (kept tracked by this README).

Expected future files:

* `dialogue_panel.png` — optional decorative background for the checkpoint dialogue panel.
* `star_reward.png` — Fatima's reward icon (نجمة الفرح).
* `heart_courage.png` — Zainab's reward icon (قلب الشجاعة).
* `key_path.png` — Jomana's reward icon (مفتاح الطريق).
* `shield_icon.png` — optional icon for Zainab's real shield effect (v0.9).
* `boost_icon.png` — optional icon for Jomana's real boost effect (v0.9).

All are optional — the checkpoint panels currently work with text only. See `docs/ASSET_FOLDER_MAP.md` for milestone mapping.

Do not create placeholder/fake PNG files here. Put real generated assets here when ready.
