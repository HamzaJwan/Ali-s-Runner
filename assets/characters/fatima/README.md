# Fatima

Expected file: `fatima_helper.png`

Purpose: Fatima checkpoint helper image (used in the v0.65/v0.67 Fatima cinematic checkpoint).

Requirements: transparent PNG, single character only, Fatima seated (she is a newborn and cannot stand/jump/crawl), calm/breathing or gently smiling, reaching out one hand offering the glowing star (نجمة الفرح), no background baked in, no text, no UI. See `docs/ANIMATION_AND_ASSET_PLAN.md` ("Fatima Helper Portrait — Ready Prompt") for the full ready-to-use image-generation prompt.

Note: a file named `fatima_helper.png` already exists at this path, but it is a real photo (460x319, opaque, with background), not the transparent game-style asset described above. Because the file exists and loads successfully, the game is currently displaying this photo directly (scaled into the checkpoint panel and the in-scene NPC sprite) instead of the text placeholder — it is not being skipped. Replace it with a proper transparent PNG matching the requirements above when ready.

Do not create placeholder/fake PNG files here. Put the real generated asset here when ready.
